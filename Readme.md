# 壁紙メーカー

写真フォルダから画像を読み込み、コルクボードに写真を押しピンで留めたような壁紙画像を自動生成するデスクトップアプリです。

CustomTkinter でダークモード風の画面を作り、Pillow で画像の合成・回転・保存を行います。

## 主な機能

- 写真フォルダの選択
- JPG / JPEG / PNG 画像の読み込み
- シームレスなコルク画像を背景として読み込み、壁紙全体にタイル表示
- 1920x1080、2560x1440、3840x2160 の壁紙解像度に対応
- コルク背景画像を指定しない場合は、自動生成のコルク風テクスチャを使用
- 写真のランダム配置と -15 度から +15 度程度の回転
- グリッドのセルへ写真を1枚ずつ割り当て、重なりを抑えた配置
- 写真枚数と壁紙の縦横比に応じてグリッドを自動調整し、余白を抑制
- 「再生成」ボタンによる配置のシャッフル
- 白いポラロイド風フレームとドロップシャドウ
- 赤、黄色、青の押しピン風グラフィック
- 生成結果のプレビュー
- JPG または PNG 形式で保存

## 必要な環境

- Windows / macOS / Linux
- Python 3
- CustomTkinter
- Pillow

Tkinter は CustomTkinter が内部で利用する Python の標準ライブラリです。環境によっては別途 Tkinter の導入が必要です。

## インストール

プロジェクトフォルダで次のコマンドを実行してください。

```bash
python -m pip install customtkinter Pillow
```

Windows で `python` コマンドが使えない場合は、次の形式も利用できます。

```bash
py -m pip install customtkinter Pillow
```

## 起動方法

プロジェクトフォルダで次のコマンドを実行します。

```bash
python app.py
```

Windows で `python` の代わりに `py` を使う場合:

```bash
py app.py
```

## 使い方

1. 「フォルダを選択」をクリックします。
2. JPG、JPEG、または PNG 写真が入ったフォルダを選択します。
3. 必要に応じて「コルク背景を選択」をクリックし、背景に使うコルク画像を選択します。
4. 「壁紙解像度」から出力サイズを選択します。
5. 「生成」をクリックしてプレビューを作成します。
6. 配置を変えたい場合は「再生成」をクリックします。
7. 「壁紙を保存」をクリックし、保存先とファイル形式を選択します。

写真はフォルダ内からランダムに選ばれ、グリッド状のセルへ1枚ずつ配置されます。写真が15枚以上ある場合は、毎回8〜15枚が使用されます。15枚未満の場合は、フォルダ内の写真がすべて使用されます。セル内の位置と角度は生成ごとに変わります。

### コルク背景画像について

「コルク背景を選択」で JPG / JPEG / PNG 画像を指定できます。画像は壁紙全体を埋めるように横方向・縦方向へ繰り返し配置されます。継ぎ目を目立たせないため、上下左右が自然につながるシームレステクスチャを使用してください。

背景画像を選択しない場合は、アプリ内で生成したコルク風テクスチャが使われます。

## ファイル構成

```text
05_壁紙メーカー/
├─ app.py       # アプリ本体
└─ Readme.md    # この説明書
```

## トラブルシューティング

### `No module named 'PIL'` と表示される

必要なPythonパッケージがインストールされていません。次のコマンドを実行してください。

```bash
python -m pip install customtkinter Pillow
```

複数の Python をインストールしている場合は、アプリ起動に使用する Python と同じ実行ファイルで Pillow をインストールしてください。

### 写真が見つからないと表示される

選択したフォルダに、拡張子が `.jpg`、`.jpeg`、または `.png` の画像を入れてください。

### 生成に時間がかかる

4K（3840x2160）を選択した場合や、写真の枚数が多い場合は、フルサイズ画像の合成に時間がかかることがあります。生成中は画面下部のステータス表示を確認してください。

## 注意事項

- 同じフォルダから生成しても、写真の配置や回転は毎回変わります。
- 出力画像は壁紙用のRGB画像として保存されます。
- 元の写真ファイルは変更されません。